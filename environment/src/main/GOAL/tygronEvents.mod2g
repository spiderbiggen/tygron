use dummy as knowledge.

module elevatorEvents {
	%%%%% Percept Processing %%%%%
	
	
	if  percept(stakeholders(X))  then insert( stakeholders(X) ).
	if percept(settings(X)) then insert(settings(X)).
	if percept(functions(X)) then insert(functions(X)).
}
