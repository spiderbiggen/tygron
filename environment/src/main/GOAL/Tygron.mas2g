use "tygronenv-1.0.0-jar-with-dependencies.jar" as environment 
	with map = "testgoalmap" .

define tygron as agent {
	use tygron as main module.
	use tygronEvents as event module.
	use tygronInit as init module.
}

launchpolicy {
	when * launch tygron.
}
